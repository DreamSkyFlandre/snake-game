#ifndef CONSTANTS_H
#define CONSTANTS_H

// --- 配置参数 ---
const int BLOCK_SIZE = 30; // 每个方块的大小
const int WINDOW_WIDTH = 1200; // 窗口宽度
const int WINDOW_HEIGHT = 900; // 窗口高度
const float MOVE_DELAY = 0.1f; // 蛇的速度
const float MAX_VOLUME = 50.f;  // 最大音量
const float FADE_SPEED = 30.f;  // 渐变速度 (每秒改变多少音量)

#endif // CONSTANTS_H